apiVersion: v1
kind: Service
metadata:
  name: {{ include "appset_repro.fullname" . }}
  labels:
    {{- include "appset_repro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appset_repro.selectorLabels" . | nindent 4 }}
